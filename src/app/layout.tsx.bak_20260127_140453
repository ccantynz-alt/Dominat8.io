import "./globals.css";
import "../styles/aurora.css";
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Dominat8",
  description: "AI website automation builder",
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}