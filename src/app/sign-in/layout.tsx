import type { Metadata } from "next";

export const metadata: Metadata = { title: "Sign in — Dominat8.io" };

export default function SignInLayout({ children }: { children: React.ReactNode }) {
  return children;
}
